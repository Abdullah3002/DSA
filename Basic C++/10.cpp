#include<bits/stdc++.h>
using namespace std;

int main() {

      int arr[5]; // if you want to store decimal number then replace with double instead of int

      cin>>arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4];

      arr[3] += 10;
      cout<<arr[3];

    return 0;
}